function[sign] = angle_direction(a, b, theta_y)
    temp_cross = cross(a, b);
    sign = 1;
    if temp_cross == 0
        sign = 0;
    elseif 0 < theta_y < pi
        if temp_cross(2) < 0
            sign = 1;
        else
            sign = -1;
        end
        
end